<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brightplate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Brightplate">
        <activity
            android:name=".controllers.InventoryViewActivity"
            android:exported="false" />
        <activity
            android:name=".controllers.InventoryEditActivity"
            android:exported="false" />
        <activity
            android:name=".controllers.TestActivity"
            android:exported="false" />
        <activity
            android:name=".controllers.InventoryActivity"
            android:exported="true" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>